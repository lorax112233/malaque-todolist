# Shadrach Malaque’s To-Do List

Simple HTML/CSS/JavaScript To-Do app built for Shadrach Malaque.

Usage:

- Open `index.html` in your browser.
- Add tasks with the input and `Add` button or press Enter.
- Click a task to mark it complete (strikethrough).
- Use the `Delete` button to remove a task.

Tasks persist in `localStorage` under the key `shadrach-todo-tasks-v1`.
